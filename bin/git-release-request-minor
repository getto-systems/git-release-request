#!/bin/bash

. git-release-functions.sh

git_release_request_minor_main(){
  git create-work-branch "$@" && \
  git commit --allow-empty -m "$(git_release_request_minor_version_up)" && \
  git pub
}

git_release_request_minor_main "$@"
