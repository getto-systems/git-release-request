#!/bin/bash

. git-release-functions.sh

git_release_request_major_main(){
  git create-work-branch "$@" && \
  git commit --allow-empty -m "$(git_release_request_major_version_up)" && \
  git pub
}

git_release_request_major_main "$@"
